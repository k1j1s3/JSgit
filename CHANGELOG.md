# Changelog

## Unreleased

## Full Equipment Effects v1

- 장비별 STR, DEX, INT, WIS, CON, CHA, 최대 HP, 최대 MP, AC 효과 재계산 추가
- 같은 슬롯의 장비 교체 시 이전 효과 제거 후 새 효과 적용
- 로그인·재접속 시 SQLite 장착 슬롯에서 능력치 자동 복원
- `.equipment`/`.gear` 명령으로 서버에 저장된 장착 슬롯 조회
- 자동 테스트 24개와 LDPlayer 실화면 검증 완료 (AC 8, HP 100/110)

## Armor & Loadout v1

- 슬롯 기반 `equipment` SQLite 테이블과 자동 마이그레이션 추가
- 실제 캡처 장갑 `80581`을 런타임 인벤토리와 화면 터치에 연결
- 방어구 장착·해제, 슬롯 교체 기반 구조와 재접속 영속 저장 추가
- 장갑 방어 보정으로 AC 10↔8 재계산 및 캐릭터 UI 동기화
- 단위·통합 자동 테스트 22개와 LDPlayer 실화면 검증 완료

## Inventory & Equipment v1

- 자동/수동 타이틀 화면 진입 중 로그인 소켓이 끊기지 않도록 초기 제한 시간 확대
- Windows 백그라운드 로그에서 중국어 콘텐츠명이 CP949 오류로 세션을 끊지 않도록 UTF-8 출력 고정
- 인벤토리 상세 보기 중 추가 `0x15`를 월드 ACK로 오인해 UI 선택을 초기화하던 재전송 제거
- LDPlayer `com.tuhota.tuhota` 월드 진입·인벤토리 스크린샷 자동화 스모크 테스트 추가
- 캡처 장비의 상세정보 연결 키(field 3)를 보존해 상세 버튼 로컬 조회 복구
- 장착 플래그로 오인했던 field 5를 원래 아이템 사용 유형으로 보존
- 캡처 무기의 객체 ID `1431489`까지 보존해 클라이언트 장비·상세 캐시 참조 복구
- 캡처 `+6` 강화 수치를 캐릭터 저장과 실제 전투 데미지에 반영
- `.item ID`로 소형/대형 대상 강화 포함 공격력 조회 지원
- v1.2: 월드 초기화의 캡처 인벤토리 전송 자체를 제외해 더미 21개 생성 방지
- v1.2: 잘못된 임시 `item_id=1`을 실제 캡처 장비 `292532 +6`으로 마이그레이션
- v1.2: 캡처 장비의 아이콘·강화·옵션 설명을 보존한 런타임 템플릿 재작성
- v1.1: 캡처 인벤토리의 더미 아이템을 제거하고 런타임 저장 아이템만 전송
- v1.1: 실제 인벤토리 터치 `0x1C`를 장착·해제·소모품 사용에 연결
- v1.1: 아이템 객체 ID 매핑과 장착 상태 비트, UI 새로고침 추가
- 재접속 가능한 무기 장착·해제 영속 상태 추가
- 인벤토리 보유 여부와 DB 무기 분류를 이용한 장착 검증 추가
- 장착 해제 상태의 기본 맨손 데미지 처리 추가
- 스타터 무기를 기존 계정에도 중복 없이 지급
- 소모품 수량 차감, HP 회복, 최대 HP 사용 방지 추가
- `.equip`, `.unequip`, `.use` 채팅 명령과 UI 재동기화 연결
- 인벤토리·장비 통합 테스트를 포함해 자동 테스트 15개로 확대

- GitHub 작업을 위한 저장소 구조 정리
- 최신 Step 10B를 기본 서버로 지정
- 캡처, 로그, APK, 캐시 및 로컬 데이터베이스 제외 규칙 추가
- 프로젝트 상태와 다음 구현 순서 문서화

## UI Sync v1

- 정상 캡처 기반 56바이트 캐릭터 상태 `0x0E` 인코더 추가
- 레벨, 경험치, 6개 능력치, HP/MP, AC UI 동기화
- 정상 `0x55/0x024C` 인벤토리 스냅샷을 보존하면서 서버 아이템 추가
- 사망 보상 직후와 재접속 월드 준비 완료 시 UI 전체 동기화
- 기존 런타임 DB에 스탯·HP·MP·성향 열을 자동 추가하는 마이그레이션
- UI 패킷 구조 자동 테스트 추가

## World/Rewards v1

- 정상 캡처에서 확인한 `0xB9` 객체 제거 패킷 연결
- 사망 모션 후 시체 제거와 설정 기반 자동 리스폰 추가
- 리스폰 시 몬스터 HP와 공격 가능 상태 초기화
- 보상 자동 획득 및 게임 내 보상 채팅 알림 추가
- `.status`, `.inventory`, `.inv`, `.help` 채팅 명령 추가
- 객체 수명주기와 명령 출력 자동 테스트 추가

## Core Server v1

- 플레이어, 몬스터, 공격 결과 및 드롭 도메인 모델 추가
- DB 무기 데미지와 플레이어 STR·DEX·레벨, 몬스터 AC 기반 전투 엔진 추가
- HP 감소, 명중, 치명타, 사망, 경험치, 레벨업 및 드롭 처리 추가
- 캐릭터 위치·경험치·인벤토리용 런타임 SQLite 저장소 추가
- 사망 시 확인된 객체 액션 패킷으로 클라이언트 사망 모션 연결
- 전투와 영속 저장 자동 테스트 추가

## Step 10F

- 몬스터 패킷 템플릿과 DB 조회 대상을 사나운 멧돼지 `npc_id=14464`로 통일
- 서버 응답 직후에도 유지되는 약 8초의 이동 대기가 클라이언트 내부 월드 준비 시간임을 확인

## Step 10E

- 반복 월드 ACK 응답 직후 최종 `0x61` 월드 이동 잠금 해제 패킷 재전송
- 필드 준비 전에 전송된 `0x61`을 클라이언트가 무시하는 경우 처리

## Step 10D

- 필드 표시 후 클라이언트가 보내는 반복 월드 상태 ACK `0x15` 처리
- 반복 ACK에 즉시 응답하여 약 8초의 클라이언트 이동 잠금 타임아웃 제거
- 콘솔 안내에 현재 서버 버전이 자동 표시되도록 수정

## Step 10C

- 월드 초기화 마지막 패킷 전의 잔여 2초 대기 제거
- 패킷 송수신 로그에 세션 시작 기준 경과시간 추가
- 실행 배너에 현재 테스트 버전 표시

## Step 10B

- 월드 ACK 이후 5초 대기 제거
- 몬스터 생성 이전 1초 대기 제거
- 월드 진입 직후 이동 요청을 받을 수 있도록 수신 루프 진입 시점 개선
- Step 10의 공격/피격 모션 처리 유지

## Step 10

- 공격 요청에 플레이어 공격 액션과 몬스터 피격 액션 응답 추가
- 실제 HP 감소, 사망, 경험치, 드롭은 의도적으로 보류

## Step 9E

- 정상 세션에서 확인한 필드 몬스터 패킷을 기반으로 몬스터 생성
- 사나운 멧돼지 데이터와 wire ID 변환 관계 확인
