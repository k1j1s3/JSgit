# Roadmap

## 현재 기준선: Hunting Zone Pack v1

- 월드 초기화 후 불필요한 5초 대기 제거
- 몬스터 생성 전 불필요한 1초 대기 제거
- 이동 요청 ACK 처리
- 실제 필드 몬스터 패킷 템플릿 기반 생성
- 공격 요청 수신 후 플레이어 공격/몬스터 피격 액션 전송
- 월드 초기화 패킷의 캡처 시점 대기를 재생하지 않도록 개선
- 필드 진입 후 반복되는 월드 상태 ACK 처리
- 클라이언트 필드 준비 완료 시점에 최종 월드 잠금 해제 패킷 전송
- 몬스터 패킷 템플릿과 DB NPC 데이터를 사나운 멧돼지로 통일
- 플레이어·몬스터 상태 객체와 전투 엔진 분리
- 무기 데미지, STR, DEX, 레벨, 몬스터 AC 기반 계산
- HP 감소, 사망, 경험치, 레벨업, 드롭 적립
- 캐릭터 위치·경험치·인벤토리 SQLite 저장
- 자동 테스트 추가
- 사망 객체 제거와 몬스터 자동 리스폰
- 자동 획득 드롭과 보상 알림
- 게임 내 상태·인벤토리 조회 명령
- 캐릭터 정보 UI의 레벨·경험치·능력치 동기화
- 인벤토리 UI에 서버 자동 획득 아이템 동기화
- 재접속 시 저장 상태와 UI 재동기화
- 주기적 heartbeat 응답과 장시간 세션 유지
- 스타터 장비를 인벤토리에 중복 없이 지급
- 인벤토리 기반 무기 장착·해제와 영속 저장
- 장착 무기를 전투 데미지 계산에 즉시 반영
- 소모품 수량 차감·HP 회복·UI 재동기화
- 캡처 무기 장착 UI와 원본 객체·상세 참조 보존
- 무기 강화 수치 영속 저장과 실제 전투 데미지 반영
- 강화 포함 소형·대형 공격력 서버 조회
- 장비 슬롯별 방어구 장착·해제와 중복 슬롯 교체 기반 구조
- 장착 방어구 및 강화 기반 AC 재계산과 영속 저장
- 실제 장갑 아이콘·장착 UI·화면 터치·재접속 검증

## 확인된 클라이언트 동작

- 월드 상태 ACK와 잠금 해제 응답은 즉시 처리됨
- 첫 이동 요청은 응답 약 8초 뒤 클라이언트에서 생성됨
- 따라서 이 구간은 서버 지연이 아닌 클라이언트 내부 월드 준비 시간으로 취급

## 다음 작업

현재 장비 효과 엔진은 STR·DEX·INT·WIS·CON·CHA·최대 HP·최대 MP·AC와 같은 슬롯 교체 및 재접속 복원을 지원합니다. 다음 단계에서는 캡처로 확인된 추가 장비 템플릿을 연결하고, 플레이어 피격·방어 계산과 바닥 드롭/수동 획득을 통합합니다.

1. 추가 방어구 템플릿 확보와 전체 장비 슬롯 확장
2. 클라이언트 원형 사망·부활 패킷 캡처 및 안전한 부활 위치 이동
3. 캡처 기반 바닥 아이템 표시 패킷 연결
4. 다중 객체 스폰 관리자와 맵 단위 월드 인스턴스의 서버 연결
5. 거울의 숲 목적지 월드 초기화 패킷 확보와 지도 이동 RPC 연결
6. 패킷 상수, 암호화와 세션 라우터 추가 분리
7. 패킷 인코딩/디코딩 회귀 테스트 확대

각 단계는 자동 테스트와 LDPlayer 화면 검증을 먼저 수행하고, 통합본에서 사용자 최종 확인을 진행합니다.
